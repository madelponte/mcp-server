# MCP server framework (FastMCP v3 + streamable-http transport).
# fastmcp depends on the `mcp` SDK, so it is pulled in transitively.
fastmcp>=3.4.2
starlette>=0.27
uvicorn>=0.31.1

# Config / settings
pydantic>=2.13
pydantic-settings>=2.14

# Shared HTTP / async
httpx>=0.28
anyio>=4.14

# Content-Encoding decoders. httpx only decodes (and only advertises in its
# Accept-Encoding) the compression schemes it can import: with these installed it
# adds Brotli ("br") and Zstandard ("zstd"). Many CDNs return those, and
# advertising the full set real browsers send makes a direct fetch look less like
# a bot, so fewer pages fall through to the FlareSolverr/Wayback fallbacks.
brotli>=1.1
zstandard>=0.23

# Agentic Web Search
beautifulsoup4>=4.15
lxml>=6.1
markdownify>=1.2

# Stock Data
requests>=2.34
yfinance>=1.4.1

# YouTube Transcript
youtube-transcript-api>=1.2.4

# Development / test dependencies (not needed to run the server).
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Test runner. The suite is offline (httpx MockTransport + monkeypatching) and
# drives async code with asyncio.run, so no async pytest plugin is required.
pytest>=8
